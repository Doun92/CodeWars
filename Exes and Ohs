def xo(s):
    listex = []
    listeo = []
    for n in s:
        if n == 'x' or n == 'X':
            listex.append(n)
        elif n == 'o' or n == 'O':
            listeo.append(n)
    if len(listex) == len(listeo):
        return True
    else:
        return False
